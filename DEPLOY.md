# DEPLOY.md — putting Rgi Service online

A staging deployment the client can browse. Three pieces:

| Piece | Host | Why there |
|---|---|---|
| `apps/web` — Next.js storefront + admin | **Vercel** | SSR/ISR, built for Next |
| `apps/api` — NestJS REST API | **Render** | a long-running Node server; not a serverless fit |
| MongoDB | **Atlas** | already live, already seeded |

> **Order matters.** The web app calls the API **server-side** (home, category and product
> pages are ISR). If `NEXT_PUBLIC_API_URL` is wrong, the site renders its shell and shows
> *« Le catalogue est momentanément indisponible »* — deploy the API first.

---

## 1. Atlas — let Render connect

Render's free plan has no fixed outbound IP, so the cluster must accept any address.

**Atlas → Network Access → Add IP Address → `0.0.0.0/0`.**

That is safe only because the connection string itself is the credential: keep the database
user's password long and random, and never commit `.env`. If you later move the API to a
host with a static IP (or Render's paid static-outbound add-on), narrow this immediately.

The cluster is already seeded — 16 categories, 76 attribute definitions, 12 compatibility
rules, 43 products. **Do not re-run `npm run seed` against it**; it is idempotent but there
is nothing to gain.

---

## 2. Render — the API

1. **New → Blueprint**, pick the `rgi-service` repo. Render reads `render.yaml` and creates
   the `rgi-service-api` service.
2. Fill in the values it marks as required:

   | Variable | Value |
   |---|---|
   | `MONGODB_URI` | the Atlas connection string (from your local `.env`) |
   | `CLOUDINARY_CLOUD_NAME` / `_API_KEY` / `_API_SECRET` | from your local `.env` |
   | `SEED_ADMIN_EMAIL` / `SEED_ADMIN_PASSWORD` | the real admin login |
   | `CORS_ORIGINS` | leave as `http://localhost:3000` for now — step 4 replaces it |

   `JWT_ACCESS_SECRET` and `JWT_REFRESH_SECRET` are generated by Render. Rotating them
   logs everyone out, which is the correct behaviour for a fresh deployment.
3. Deploy. When it goes live you get a URL like `https://rgi-service-api.onrender.com`.
4. Check it: `https://rgi-service-api.onrender.com/api/v1/health` → `{"status":"ok"}`, and
   `…/api/v1/products?limit=1` → a product.

> ⚠ **Free plan sleeps after 15 minutes idle.** The next request then takes ~50 s while the
> service wakes. Before showing the client, open the health URL once and wait for it to
> answer — everything after that is fast. If the demo needs to be reliably instant for
> someone clicking a link cold, the $7/month Starter plan removes the sleep.

---

## 3. Vercel — the storefront

1. **Add New → Project**, import the `rgi-service` repo.
2. **Root Directory → `apps/web`.** This is the only setting that needs changing: Vercel
   then detects Next.js and installs from the workspace root, so `@rgi/types` and
   `@rgi/config-engine` resolve.
3. Environment variables (Production **and** Preview):

   | Variable | Value |
   |---|---|
   | `NEXT_PUBLIC_API_URL` | `https://rgi-service-api.onrender.com/api/v1` |
   | `NEXT_PUBLIC_SITE_URL` | `https://rgiservice.ma` once section 6 is done; `https://rgi-service.vercel.app` until then |

   `NEXT_PUBLIC_SITE_URL` is what canonical URLs, `sitemap.xml`, Open Graph and the
   JSON-LD are built from — wrong here means wrong metadata everywhere.
4. Deploy.

---

## 4. Close the loop — CORS

The API must now accept the browser origin Vercel gave you. On Render, set:

```
CORS_ORIGINS=https://rgi-service.vercel.app,https://*.vercel.app,http://localhost:3000
```

- the first entry is production,
- the wildcard covers **preview deployments** — Vercel gives every push its own hostname,
  and without it every preview build fails its API calls,
- localhost keeps your machine working.

Save; Render redeploys. Then **redeploy the Vercel project** if you changed
`NEXT_PUBLIC_SITE_URL` after the first build — `NEXT_PUBLIC_*` values are baked in at build
time, not read at runtime.

---

## 5. Check it end to end

- `/` — hero carousel, categories, real products with prices
- `/composants/cartes-graphiques` — listing with working facets
- `/configurateur-pc` — pick a case, later steps narrow to compatible parts
- add to cart → `/panier` → `/commande` (COD; card is refused until CMI credentials exist)
- `/admin` — redirects to the login, then the dashboard

---

## 6. Going live on `rgiservice.ma`

**The domain does not point here.** As decided on 2026-08-24 the storefront is a static
export hosted on Hostinger shared hosting, and `rgiservice.ma` points at Hostinger. The
whole cutover — including the MX record that must be split before the apex moves, or
`contact@rgiservice.ma` goes dark — lives in **`DEPLOY_HOSTINGER.md`**. Do not repoint DNS
from this file.

What stays here:

| Piece | Host | Address |
|---|---|---|
| Storefront | Hostinger | `https://rgiservice.ma` |
| **Admin dashboard** | **Vercel** | `https://rgi-service.vercel.app/admin` |
| API | Render | `https://rgi-service-api.onrender.com/api/v1` |
| Database | Atlas | — |

The admin cannot be part of the static export: its pages take ids that do not exist at
build time, and its session gate is Next middleware, which needs a server. So the Vercel
project keeps running purely to serve `/admin` for staff. That is a handful of people on a
free plan.

### 6.1 — CORS must list every origin that calls the API

Browsers call the API from **two** places now — the shop on Hostinger and the admin on
Vercel. On Render → `rgi-service-api` → Environment:

```
CORS_ORIGINS=https://rgiservice.ma,https://www.rgiservice.ma,https://rgi-service.vercel.app,https://*.vercel.app,http://localhost:3000
```

Miss the first entry and the shop's cart and filters fail silently. Miss the Vercel entries
and staff cannot log in.

### 6.2 — The Vercel project's own variables

`NEXT_PUBLIC_SITE_URL` on Vercel should stay `https://rgi-service.vercel.app`. It is only
used to build canonical URLs and JSON-LD, and the admin is `noindex` — pointing it at
`rgiservice.ma` would make the admin advertise canonicals for pages Hostinger serves.

### 6.3 — Search Console

Add the property for `rgiservice.ma` (the Hostinger site) only, and submit
`https://rgiservice.ma/sitemap.xml`. The Vercel host serves no indexable page any more.

### Before you call it launched

- **Render's free plan sleeps after 15 minutes idle** — the next request waits ~50 s. The
  shop's cart and checkout go through this API, so a sleeping API means a customer watching
  a spinner at the moment they try to pay. **Render Starter, $7/month**, removes it and is
  the first money this project should spend.
- Every catalogue change needs a static rebuild and re-upload (`DEPLOY_HOSTINGER.md` §7).

### Optional: `api.rgiservice.ma`

Pointing a subdomain at Render (Render → Settings → Custom Domain, plus a `CNAME api →
rgi-service-api.onrender.com` at HeberJahiz) means the shop can leave Render later without
rebuilding the storefront, stops `onrender.com` showing in the network tab, and gives CMI a
first-party callback host. It changes `NEXT_PUBLIC_API_URL`, so it needs a fresh static
build — cheapest to do in the same pass as the first upload.

---

## Not production-ready yet

This is a **staging/demo** deployment. Before a real launch:

- **CMI** — no merchant credentials, so card payment is refused by design. COD works.
- **Product photography** — the 43 catalogue images are manufacturer press shots standing
  in for the shop's own photos. Replace them through `/admin` (Cloudinary upload is live).
- **Cloudinary account is shared** with another project. Move Rgi Service onto the client's
  own account before launch: a shared free plan means another project's traffic can throttle
  this shop's images, and the client cannot be handed credentials that unlock someone else's
  media.
- **NAP** — the footer's address, e-mail and opening hours are placeholders, and there is no
  `LocalBusiness` JSON-LD until they are real.
- **Analytics** — no GA4 and no consent banner yet.
- **Domain** — `rgiservice.ma` is registered but still parked at HeberJahiz. **Section 6**
  is the cutover; until it is done, `.vercel.app` is what Google would index.
